<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/splash_nav_graph"
    app:startDestination="@id/splashFragment2">

    <fragment
        android:id="@+id/splashFragment2"
        android:name="iti.mad.marketly.presentation.SplashFragment"
        android:label="fragment_splash"
        tools:layout="@layout/fragment_splash" >
        <action
            android:id="@+id/action_splashFragment2_to_deciderActivity"
            app:destination="@id/deciderActivity" />
    </fragment>
    <activity
        android:id="@+id/deciderActivity"
        android:name="iti.mad.marketly.onboarding.DeciderActivity"
        android:label="DeciderActivity" />
</navigation>